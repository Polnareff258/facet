# 行尾约定 —— 这里不是洁癖，是正确性问题
#
# 背景：Windows 上签出时 git 默认会把 LF 转成 CRLF。
# 这对 .sh 是致命的：CRLF 会让 Linux 报
#     bash: ./start.sh: /usr/bin/env: bad interpreter: No such file or directory
# 因为解释器路径末尾多了一个 \r。
#
# 反过来，.cmd/.ps1 在 Windows 上需要 CRLF 才不会出现奇怪的解析问题。
# 所以按文件类型分别指定，而不是全局 auto。

* text=auto eol=lf

# Windows 专用脚本：强制 CRLF
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# POSIX shell 脚本：强制 LF（含 shebang，不能被 CRLF 污染）
*.sh   text eol=lf
start.sh text eol=lf

# 二进制资源不做行尾转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.gz   binary
*.zip  binary
*.db   binary

# 锁文件不参与 diff 统计
package-lock.json linguist-generated=true
